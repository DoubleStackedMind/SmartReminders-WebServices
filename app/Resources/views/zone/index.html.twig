{% extends 'base.html.twig' %}

{% block body %}
    <h1>Zones list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Log</th>
                <th>Lat</th>
                <th>Radius</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for zone in zones %}
            <tr>
                <td><a href="{{ path('zone_show', { 'id': zone.id }) }}">{{ zone.id }}</a></td>
                <td>{{ zone.name }}</td>
                <td>{{ zone.log }}</td>
                <td>{{ zone.lat }}</td>
                <td>{{ zone.radius }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('zone_show', { 'id': zone.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('zone_edit', { 'id': zone.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('zone_new') }}">Create a new zone</a>
        </li>
    </ul>
{% endblock %}
