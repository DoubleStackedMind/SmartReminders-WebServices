{% extends 'base.html.twig' %}

{% block body %}
    <h1>Triggger</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ triggger.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ triggger.name }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ triggger.icon }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('triggger_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('triggger_edit', { 'id': triggger.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
